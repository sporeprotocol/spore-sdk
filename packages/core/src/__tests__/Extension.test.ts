import { describe, it } from 'vitest';

describe('Extension', function () {
  it('How should it interact with the apis', function () {
    function mint(props: any) {}

    mint({
      nftData: {},
      toLock: {},
    });
  });
});
